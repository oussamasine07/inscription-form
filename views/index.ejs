<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/ee78fb64a1.js" crossorigin="anonymous"></script>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=PT+Sans:wght@400;700&family=Playfair+Display:wght@400;500;700&family=Roboto+Condensed:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <title>inscription</title>
</head>
<body>

    <!-- services presentation starts -->
    <section class="formations-presentation py">
        <div class="sign-up-section container-formation padding-20">
            <!-- <h1 class="heading-main">inscrivez vous</h1>
            <div class="inscription-btn-box padding-40">
                <h1 class="heading-secondary">vous êtes-vous déjà inscrit?</h1>
                <button class="sign-up-btn already-sign-up">oui, déja inscrit</button>
                <button class="sign-up-btn sign-me-up">non, inscrivez mois</button> -->
                
                <!-- new sign up form starts -->
                <div class="new-sign-up-form valider_inscription" id="new-user">
                    
                    <form action="/inscription" method="POST" enctype="multipart/form-data">
                        <div class="form-border">
                            <h2 class="heading-title-formSignup">identification du stagiaire</h2>
                            <% if ( errors.length > 0 ) { %>
                                <% errors.forEach(error => { %>
                                    <% if (error.param == "userExist") { %>
                                        <div class="already-warning">
                                            <%= error.msg %>
                                            <div class="close-warning">
                                                <i class="far fa-window-close"></i>
                                            </div>
                                        </div>
                                    <% } %>
                                <% }) %>
                            <% } %>
                            
                            <div class="row-input-2">
                                <div class="input-group">
                                    <label class="label-name" for="nom">* nom:</label>
                                    <input type="text" placeholder="Nom" name="nom" class="input-form"
                                        <% if ( errors.length > 0 ) { %>
                                            <% errors.forEach(error => { %>
                                                <% if (error.param == "nom") { %>
                                                    style="border: 2px solid var(--main-red);"
                                                <% } %>
                                            <% }) %>
                                        <% } %>
                                        value="<%= nom %>"
                                    >
                                    <% if ( errors.length > 0 ) { %>
                                        <% errors.forEach(error => { %>
                                            <% if (error.param == "nom") { %>
                                                <div class="feed-back"> <%= error.msg %></div>
                                            <% } %>
                                        <% }) %>
                                    <% } %>
                                </div>
                                <div class="input-group">
                                    <label class="label-name" for="prenom">* prénom:</label>
                                    <input type="text" placeholder="Prénom" name="prenom" class="input-form"
                                        <% if ( errors.length > 0 ) { %>
                                            <% errors.forEach(error => { %>
                                                <% if (error.param == "prenom") { %>
                                                    style="border: 2px solid var(--main-red);"
                                                <% } %>
                                            <% }) %>
                                        <% } %>
                                        value="<%= prenom %>"
                                    >
                                    <% if ( errors.length > 0 ) { %>
                                        <% errors.forEach(error => { %>
                                            <% if (error.param == "prenom") { %>
                                                <div class="feed-back"> <%= error.msg %></div>
                                            <% } %>
                                        <% }) %>
                                    <% } %>
                                </div>
                            </div>

                            <div class="row-input-1">
                                <div class="input-group">
                                    <label class="label-name" for="email">* email:</label>
                                    <input type="text" placeholder="Email" name="email" class="input-form"
                                        <% if ( errors.length > 0 ) { %>
                                            <% errors.forEach(error => { %>
                                                <% if (error.param == "email") { %>
                                                    style="border: 2px solid var(--main-red); color: var(--main-red)"
                                                <% } %>
                                            <% }) %>
                                        <% } %>
                                        value="<%= email %>"
                                    >
                                    <% if ( errors.length > 0 ) { %>
                                        <% errors.forEach(error => { %>
                                            <% if (error.param == "email") { %>
                                                <div class="feed-back"> <%= error.msg %></div>
                                            <% } %>
                                        <% }) %>
                                    <% } %>
                                </div>
                            </div>

                            <div class="row-input-2">
                                <div class="input-group">
                                    <label class="label-name" for="tele_portable">* tele portable :</label>
                                    <input type="text" placeholder="Tele Portable" name="tele_portable" class="input-form"
                                        <% if ( errors.length > 0 ) { %>
                                            <% errors.forEach(error => { %>
                                                <% if (error.param == "tele_portable") { %>
                                                    style="border: 2px solid var(--main-red);"
                                                <% } %>
                                            <% }) %>
                                        <% } %>
                                        value="<%= tele_portable %>"
                                    >
                                    <% if ( errors.length > 0 ) { %>
                                        <% errors.forEach(error => { %>
                                            <% if (error.param == "tele_portable") { %>
                                                <div class="feed-back"> <%= error.msg %></div>
                                            <% } %>
                                        <% }) %>
                                    <% } %>
                                </div>
                                <div class="input-group">
                                    <label class="label-name" for="tele_domicile"> tele domicile:</label>
                                    <input type="tel" placeholder="Tel Domicile" name="tele_domicile" class="input-form">
                                </div>
                            </div>

                            <div class="row-input-2-1">
                                <div class="input-group larg-input">
                                    <label class="label-name" for="adress_personnelle">* adress personnelle: </label>
                                    <input type="text" placeholder="Adress" name="adress_personnelle" class="input-form"
                                        <% if ( errors.length > 0 ) { %>
                                            <% errors.forEach(error => { %>
                                                <% if (error.param == "adress_personnelle") { %>
                                                    style="border: 2px solid var(--main-red);"
                                                <% } %>
                                            <% }) %>
                                        <% } %>
                                        value="<%= adress_personnelle %>"
                                    >
                                    <% if ( errors.length > 0 ) { %>
                                        <% errors.forEach(error => { %>
                                            <% if (error.param == "adress_personnelle") { %>
                                                <div class="feed-back"> <%= error.msg %></div>
                                            <% } %>
                                        <% }) %>
                                    <% } %>
                                </div>
                                <div class="input-group small-input">
                                    <label class="label-name" for="codepostal_personnelle">* code postal personnelle:</label>
                                    <input type="text" placeholder="Code Postal" name="codepostal_personnelle" class="input-form"
                                        <% if ( errors.length > 0 ) { %>
                                            <% errors.forEach(error => { %>
                                                <% if (error.param == "codepostal_personnelle") { %>
                                                    style="border: 2px solid var(--main-red);"
                                                <% } %>
                                            <% }) %>
                                        <% } %>
                                        value="<%= codepostal_personnelle %>"
                                    >
                                    <% if ( errors.length > 0 ) { %>
                                        <% errors.forEach(error => { %>
                                            <% if (error.param == "codepostal_personnelle") { %>
                                                <div class="feed-back"> <%= error.msg %></div>
                                            <% } %>
                                        <% }) %>
                                    <% } %>
                                </div>
                            </div>

                            <div class="row-input-2-1">
                                <div class="input-group larg-input">
                                    <label class="label-name" for="adress_domicile"> adress domicile: </label>
                                    <input type="text" placeholder="Adress" name="adress_domicile" class="input-form">
                                </div>
                                <div class="input-group small-input">
                                    <label class="label-name" for="codepostal_domicile">code postal domicile:</label>
                                    <input type="number" placeholder="Code postal" name="codepostal_domicile" class="input-form">
                                </div>
                            </div>

                            <div class="row-input-2">
                                <div class="input-group">
                                    <label class="label-name" for="date_de_naissance">* date de naissance:</label>
                                    <input type="date" placeholder="Date de Naissance" name="date_de_naissance" class="input-form"
                                        <% if ( errors.length > 0 ) { %>
                                            <% errors.forEach(error => { %>
                                                <% if (error.param == "date_de_naissance") { %>
                                                    style="border: 2px solid var(--main-red);"
                                                <% } %>
                                            <% }) %>
                                        <% } %>
                                        value="<%= date_de_naissance %>"
                                    >
                                    <% if ( errors.length > 0 ) { %>
                                        <% errors.forEach(error => { %>
                                            <% if (error.param == "date_de_naissance") { %>
                                                <div class="feed-back"> <%= error.msg %></div>
                                            <% } %>
                                        <% }) %>
                                    <% } %>
                                </div>
                                <div class="input-group">
                                    <label class="label-name" for="lieu_de_naissance">* lieu de naissance:</label>
                                    <input type="text" placeholder="Lieu de Naissance" name="lieu_de_naissance" class="input-form"
                                        <% if ( errors.length > 0 ) { %>
                                            <% errors.forEach(error => { %>
                                                <% if (error.param == "lieu_de_naissance") { %>
                                                    style="border: 2px solid var(--main-red);"
                                                <% } %>
                                            <% }) %>
                                        <% } %>
                                        value="<%= lieu_de_naissance %>"
                                    >
                                    <% if ( errors.length > 0 ) { %>
                                        <% errors.forEach(error => { %>
                                            <% if (error.param == "lieu_de_naissance") { %>
                                                <div class="feed-back"> <%= error.msg %></div>
                                            <% } %>
                                        <% }) %>
                                    <% } %>
                                </div>
                            </div>

                            <div class="row-input-checkbox" style="margin-top: 10px;">
                                <div class="label-name">* sex:</div>
                                <div class="input-check">
                                    <div class="input-group-checkbox">
                                        <label class="label-checkbox" for="sex">masculin</label>
                                        <input type="checkbox" name="sex" value="masculin" class="chechbox-input gender-checked male"
                                            
                                                <% if (sex == "masculin") { %>
                                                    checked="true"
                                                <% } %>
                                        >
                                    </div>
                                    <div class="input-group-checkbox">
                                        <label class="label-checkbox" for="sex">feminin</label>
                                        <input type="checkbox" name="sex" value="feminin" class="chechbox-input gender-checked female"
                                            
                                                <% if (sex == "feminin") { %>
                                                    checked="true"
                                                <% } %>
                                        >
                                        <% if ( errors.length > 0 ) { %>
                                            <% errors.forEach(error => { %>
                                                <% if (error.param == "sex") { %>
                                                    <div class="feed-back"> <%= error.msg %></div>
                                                <% } %>
                                            <% }) %>
                                        <% } %>
                                    </div>
                                </div>
                            </div>

                            <div class="row-input-2">
                                <div class="input-group">
                                    <label class="label-name" for="stagiaire_photo">* ajouter une photo:</label>
                                    <input type="file" hidden="true" placeholder="Date De Naissance:" name="stagiaire_photo" value="<%= stagiaire_photo %>" class="input-form" id="input-upload-photo">
                                    <div class="input-form"
                                        <% if ( errors.length > 0 ) { %>
                                            <% errors.forEach(error => { %>
                                                <% if (error.param == "stagiaire_photo") { %>
                                                    style="border: 2px solid var(--main-red);"
                                                <% } %>
                                            <% }) %>
                                        <% } %>
                                    >
                                        <button id="upload-photo" class="upload upload_photo">photo</button>
                                    </div>
                                    <% if ( errors.length > 0 ) { %>
                                        <% errors.forEach(error => { %>
                                            <% if (error.param == "stagiaire_photo") { %>
                                                <div class="feed-back"> <%= error.msg %></div>
                                            <% } %>
                                        <% }) %>
                                    <% } %>
                                </div>
                                <div class="input-group">
                                    <label class="label-name" for="stagiaire_cv">* ajouter votre CV:</label>
                                    <input type="file" hidden="true" placeholder="Lieu de naissance" name="stagiaire_cv" value="<%= stagiaire_cv %>" class="input-form" id="input-upload-cv">
                                    <div class="input-form"
                                        <% if ( errors.length > 0 ) { %>
                                            <% errors.forEach(error => { %>
                                                <% if (error.param == "stagiaire_cv") { %>
                                                    style="border: 2px solid var(--main-red);"
                                                <% } %>
                                            <% }) %>
                                        <% } %>
                                    >
                                        <button class="upload upload_cv" id="upload-cv">CV</button>
                                    </div>
                                    <% if ( errors.length > 0 ) { %>
                                        <% errors.forEach(error => { %>
                                            <% if (error.param == "stagiaire_cv") { %>
                                                <div class="feed-back"> <%= error.msg %></div>
                                            <% } %>
                                        <% }) %>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                        <div class="input-btns">
                            <input type="submit" class="input-form-btn next" value="suivant >>">
                        </div>
                    </form>
                </div>
                <!-- new sign up form ends -->


            </div>
        </div>
    </section>
    <!-- services presentation starts -->


    <script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
    <script src="/js/app.js"></script>
</body>
</html>